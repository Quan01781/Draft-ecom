@using ShareViewModel.DTO;
@model List<CategoryDTO>


<ul class="dropdown-menu">
    @{
        foreach (var item in Model)
        {
            <li>
                @Html.ActionLink(@item.Name, "ProductByCategory", "Product", new{ID=@item.ID,page=1}, null)
            </li>
        }
    }
</ul>
